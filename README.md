# Dora Windows

Simple starter project for the Dora Windows desktop assistant.

## How to run

1. Make sure Python 3 is installed.
2. Install dependencies:

   ```bash
   pip install -r requirements.txt
